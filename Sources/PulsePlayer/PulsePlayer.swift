// PulsePlayer — production AVPlayer toolkit for Apple platforms.
// Public API surface is re-exported via types in Core/ and UI/.

/// Library version string.
public enum PulsePlayerInfo {
    public static let version = "0.5.0"
}
