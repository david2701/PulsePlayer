name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Package requires Swift tools 6.3+ (swift-tools-version: 6.3).
# On macos-15 the default /Applications/Xcode.app is still 16.4 → Swift 6.1.
# Pin Xcode 26.x so `swift test` matches the 6.3 toolchain requirement.
env:
  DEVELOPER_DIR: /Applications/Xcode_26.3.app/Contents/Developer

jobs:
  test:
    name: swift test
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Show toolchain
        run: |
          set -euo pipefail
          echo "DEVELOPER_DIR=$DEVELOPER_DIR"
          test -d "$DEVELOPER_DIR" || {
            echo "::error::Missing $DEVELOPER_DIR — list of Xcodes:"
            ls /Applications | grep -i Xcode || true
            exit 1
          }
          xcodebuild -version
          swift --version
          if ! swift --version 2>&1 | grep -qE 'Swift version 6\.(3|4|5|6|7|8|9)'; then
            echo "::error::Need Swift 6.3+ (package tools-version 6.3)."
            exit 1
          fi

      - name: Run tests
        run: swift test

      - name: Line count guard
        run: ./Scripts/check-line-count.sh

  build-demo:
    name: build iOS demo
    runs-on: macos-15
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Show toolchain
        run: |
          set -euo pipefail
          test -d "$DEVELOPER_DIR"
          xcodebuild -version
          swift --version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate & build demo
        working-directory: Examples/PulsePlayerDemo
        run: |
          set -euo pipefail
          xcodegen generate

          DEST='platform=iOS Simulator,name=iPhone 16'
          if xcrun simctl list devices available | grep -q 'iPhone 17'; then
            DEST='platform=iOS Simulator,name=iPhone 17'
          fi
          echo "Building with -destination $DEST"

          xcodebuild \
            -project PulsePlayerDemo.xcodeproj \
            -scheme PulsePlayerDemo \
            -destination "$DEST" \
            -derivedDataPath ./DerivedData \
            build CODE_SIGNING_ALLOWED=NO
